import React from "react";
import { Typography } from "@mui/material";

const Logo = () => (
  <Typography variant="h3" component="h1" fontStyle="italic" fontWeight="500">
    words
  </Typography>
);

export default Logo;
